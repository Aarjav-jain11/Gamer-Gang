# Gamer Gang — Prototype

This repository contains a Next.js + Tailwind prototype for a dark, esports-themed gaming site scaffold.

Quick start:

```bash
npm install
npm run dev
```

Features in this scaffold:
- Dark, glassmorphism-based UI
- Tailwind CSS setup
- Basic pages and components (Hero, Featured Games, Tournaments, Community, Shop, Footer)
- Animations via Framer Motion + GSAP ready (libs listed in package.json)

Next steps:
- Run `npm install` to fetch dependencies
- Customize assets, copy real thumbnails and game data
- Wire up Discord auth and backend APIs as needed
# Gamer-Gang